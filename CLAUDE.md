# markrandyreid.github.io

Personal website for markrandyreid.com (GitHub Pages, custom domain via CNAME). Static HTML/CSS site.

## Workflow

- After every completed edit/submission, commit and push to `main` automatically (no need to ask for confirmation each time). Pushing to `main` deploys live to markrandyreid.com via GitHub Pages.
